@page
@model PCMSystem.Pages.Treasury.CategoriesModel

@{
    ViewData["Title"] = "Quản lý Danh mục";
}

<div class="container mt-5">
    <h1 class="mb-4">📂 Quản lý Danh mục Thu/Chi</h1>

    <!-- Form thêm danh mục mới -->
    <div class="card shadow mb-4 animate__animated card-hover-gradient treasury-card" tabindex="0">
        <div class="card-header bg-success text-white">
            <h5 class="mb-0">➕ Thêm Danh mục mới</h5>
        </div>
        <div class="card-body">
            <form method="post" asp-page-handler="create">
                <div class="row mb-3">
                    <div class="col-md-6">
                        <label asp-for="NewCategory.CategoryName" class="form-label">Tên Danh mục</label>
                        <input asp-for="NewCategory.CategoryName" class="form-control" placeholder="VD: Tiền phạt đi trễ" required/>
                        <span asp-validation-for="NewCategory.CategoryName" class="text-danger"></span>
                    </div>
                    <div class="col-md-4">
                        <label asp-for="NewCategory.Type" class="form-label">Loại</label>
                        <select asp-for="NewCategory.Type" class="form-select" required>
                            <option value="">-- Chọn loại --</option>
                            <option value="Thu">Thu (Nhập tiền)</option>
                            <option value="Chi">Chi (Xuất tiền)</option>
                        </select>
                        <span asp-validation-for="NewCategory.Type" class="text-danger"></span>
                    </div>
                    <div class="col-md-2">
                        <label>&nbsp;</label>
                        <button type="submit" class="btn btn-success w-100">Thêm</button>
                    </div>
                </div>
            </form>
            @if (!ViewData.ModelState.IsValid)
            {
                <div class="alert alert-warning">
                    @Html.ValidationSummary()
                </div>
            }
        </div>
    </div>

    <!-- Danh sách danh mục -->
    <div class="card shadow animate__animated card-hover-gradient treasury-card" tabindex="0">
        <div class="card-header bg-primary text-white">
            <h5 class="mb-0">Danh sách Danh mục</h5>
        </div>
        <div class="card-body">
            @if (Model.Categories.Count > 0)
            {
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead class="table-light">
                            <tr>
                                <th>Tên Danh mục</th>
                                <th>Loại</th>
                                <th>Ngày tạo</th>
                                <th class="text-center">Hành động</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var category in Model.Categories.OrderBy(c => c.Type).ThenBy(c => c.CategoryName))
                            {
                                <tr>
                                    <td><strong>@category.CategoryName</strong></td>
                                    <td>
                                        @if (category.Type == "Thu")
                                        {
                                            <span class="badge bg-success">Thu</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-danger">Chi</span>
                                        }
                                    </td>
                                    <td>@category.CreatedDate.ToString("dd/MM/yyyy")</td>
                                    <td class="text-center">
                                        <form method="post" asp-page-handler="delete" style="display: inline;">
                                            <input type="hidden" name="id" value="@category.Id"/>
                                            <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Xóa danh mục này?');">
                                                Xóa
                                            </button>
                                        </form>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="alert alert-info">Chưa có danh mục nào.</div>
            }
        </div>
    </div>

    <div class="mt-3">
        <a asp-page="./Transactions" class="btn btn-secondary">← Quay lại</a>
    </div>
</div>
