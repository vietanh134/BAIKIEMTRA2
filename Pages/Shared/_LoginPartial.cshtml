@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item d-flex align-items-center">
        <span class="badge bg-gradient bg-info text-dark me-2 animate__animated animate__fadeInDown">👋 Xin chào <b>@User.Identity?.Name!</b></span>
    </li>
    <li class="nav-item">
        <form class="form-inline d-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post">
            <button type="submit" class="btn btn-outline-danger btn-sm ms-2 animate__animated animate__pulse animate__infinite">Đăng xuất</button>
        </form>
    </li>
}
else
{
    <li class="nav-item">
        <a class="btn btn-outline-success btn-sm me-2 animate__animated animate__fadeInLeft" asp-area="Identity" asp-page="/Account/Register">Đăng ký</a>
    </li>
    <li class="nav-item">
        <a class="btn btn-outline-primary btn-sm animate__animated animate__fadeInRight" asp-area="Identity" asp-page="/Account/Login">Đăng nhập</a>
    </li>
}
</ul>
