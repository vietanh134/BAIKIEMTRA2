@page
@model PCMSystem.Pages.News.IndexModel

@{
    ViewData["Title"] = "Tin tức & Thông báo";
}

<div class="container mt-5">
    <h1 class="mb-4">📰 Tin tức & Thông báo</h1>

    <div class="mb-3">
        <a asp-page="./Create" class="btn btn-success">✍️ Đăng bài viết mới (Admin)</a>
    </div>

    <div class="row">
        @foreach (var news in Model.NewsList)
        {
            <div class="col-md-6 mb-4">
                <div class="card shadow h-100 animate__animated card-hover-gradient news-card" tabindex="0">
                    <div class="card-body">
                        <h5 class="card-title">@news.Title</h5>
                        <p class="card-text text-muted small">
                            <strong>📅 @news.PostedDate.ToString("dd/MM/yyyy HH:mm")</strong><br/>
                            <em>Bởi: @news.Author</em>
                        </p>
                        <p class="card-text">
                            @if (news.Content.Length > 200)
                            {
                                @(news.Content.Substring(0, 200) + "...")
                            }
                            else
                            {
                                @news.Content
                            }
                        </p>
                    </div>
                </div>
            </div>
        }
    </div>

    @if (Model.NewsList.Count == 0)
    {
        <div class="alert alert-info">Chưa có tin tức nào.</div>
    }
</div>
